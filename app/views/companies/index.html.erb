<h1>Empresas</h1>

<%= link_to "Nova Empresa", new_company_path, class: "btn btn-primary" %>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>#</th>
      <th style="width:1070px">Nome</th>
      <th>#</th>
    </tr>
  </thead>

  <tbody>
    <% @companies.each do |company| %>
      <tr>
        <td><%= company.id %></td>
        <td><%= company.name %></td>
        <td>
          <%= link_to 'Ver', company_path(company), class: 'btn btn-primary btn-sm' %>
          <%= link_to 'Editar', edit_company_path(company), class: 'btn btn-secondary btn-sm' %>
          <%= link_to 'Excluir', company_path(company), method: :delete, data: { confirm: 'Tem certeza?' }, class: 'btn btn-danger btn-sm' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>